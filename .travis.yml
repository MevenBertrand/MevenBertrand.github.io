language: python
branches:
  only:
  - source
install:
- pip3 install -r requirements.txt
script:
- make publish
deploy:
  provider: pages
  local_dir: ./output
  skip_cleanup: true
  target_branch: gh-pages
  on:
    branch: source